@page
@model SRAUMOAR.Pages.generales.profesiones.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Profesion[0].NombreProfesion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Profesion) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreProfesion)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ProfesionId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ProfesionId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ProfesionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
